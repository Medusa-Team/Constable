/*
 * Phase 6 network surface.  The kernel supplies the concrete protocol-v4
 * layouts; this fixture proves that every supported socket event is a
 * first-class policy reference.
 */
* socket_create * {
	return ALLOW;
}

* socket_bind_access * {
	return ALLOW;
}

* socket_connect_access * {
	return ALLOW;
}

* socket_listen_access * {
	return ALLOW;
}

* socket_accept_access * {
	return ALLOW;
}

* socket_sendmsg_access * {
	return ALLOW;
}

* socket_recvmsg_access * {
	return ALLOW;
}
